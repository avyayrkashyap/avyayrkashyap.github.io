# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS
.DS_Store

# Claude Code local state. settings.local.json records approved commands
# verbatim, which can include tokens pasted into a shell call, so it must
# never be committed to a public repo.
.claude/settings.local.json
.claude/worktrees/
